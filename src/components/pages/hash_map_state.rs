use gloo::console::log;
use yew::prelude::*;
use std::collections::HashMap;

#[function_component(HashMapState)]
pub fn test_hash_map() -> Html {
    // let hash_map = use_state(||HashMap::new());
    // let mut cloned = *hash_map.clone();
    // cloned.insert(1, "a");

    html! {
        <>
        </>
    }
}